# gql-backend/src/graphql/types/User.graphql

type User {
    id: UUID!
    username: String!
    password: String!
    email: String!
    privilege: String!
    active: Boolean!
    createdAt: DateTime!
    updatedAt: DateTime!
}
